{
    "name": "indeed-scraper-web",
    "version": "1.0.0",
    "type": "module",
    "description": "Web frontend for the Indeed job scraper Apify actor",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "start": "node server.js",
        "dev": "node --watch server.js",
        "stop": "npx kill-port 3000"
    },
    "dependencies": {
        "express": "^4.19.2",
        "dotenv": "^16.4.5"
    }
}
